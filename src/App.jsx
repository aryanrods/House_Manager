import { useState } from "react";

function App() {
  const [users, SetUsers] = useState([]);

  function AddUser(name) {
    const newUser = {
      name: name,
      balance: 0,
    };
  }

  return;
}

export default App;
